/**
 * @param points An array of points representing a polygon
 * @returns A new array of points representing the original polygon flipped horizontally over its center point
 */
const getFlippedPoints = (points) => {
  // @TODO: implement logic here
};

module.exports = getFlippedPoints;
